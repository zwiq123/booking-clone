-----MODELS-----

pricing model
bookings model
category model + seed
subcategory model + seed

-----ADDRESS----

latitude/longitude calculation
prisma.$queryRaw``;
ST_DWithin(
    ST_MakePoint(longitude, latitude))::geography,
    ...,
    radius in meters
)
ST_Distance_Sphere(
    point(longitude, latitude),
    point(...)
) AS Distance

-----MISCELLANEOUS-----

booking scraping
all seeds

host dashboard:
- properties
- reservations
- reviews

-----MECHANICS-----

change password

sortType=[near, rating, reviews, price], 
sort=[desc, asc]
rating=
minRating=
minPrice=
maxPrice=
maxDistance=
...

payment (card numbers)

bookings

-----ENDPOINTS-----

GET /properties
GET /properties/:id

POST /properties (host)
PUT /properties/:id (host)
DELETE /porperites/:id (host)

GET /properties/:id/rooms
POST /rooms (host)

POST /bookings
GET /bookings (jwt) (bookings made by you)
GET /bookings (jwt) (host) (bookings made for your properties)
...

POST /reviews (user) (restrict to users who had a booking (successful))
GET /properties/:id/reviews

...

-----SCENARIOS-----

HOUSE:
whole/single room
type
name
address
info (rooms with bed count and type, other rooms with beds, max guest count, bathroom count, kids allowed, baby beds provided, optionally size)
amenities
amenities 2 (breakfast (included/paid, types), parking (free/paid, where, reservation, public/private))
languages
rules (smoking, parties, pets (paid extra), check in/out times)
additional info (property, owner, surroundings)
photos
payment
price per night
price info (max cancellation time, group size price reduciton, age price reduction)
private person/business details


HOTEL:
type
name
address
rating
amenities
amenities 2
languages
rooms (type, count, beds, guest count, optional size, smoking, amenities, name, pricing (default, group/age price reduction))
payment
business details


-------------

-----AFTER-----
delete register messages 1,2
check every validation case
spelling
file structure
try catch


-----PLAN-----
images upload [x]
everything about properties host-side (get, delete, update (rooms, amenities, info))
properties user-side (get, filter, sort)

pricing

bookings (model, creation, saving the spot (when user start booking))
payment (card info, etc)
displaying bookings (user, host)
bookings management

reviews creation (check if user had a booking, limit to one)
get reviews, edit, delete, calculate rating when displaying properties

